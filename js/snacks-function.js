// Creiamo una funzione che, ricevuta una parola,
//  la restituisca con la iniziale maiuscola e il resto in minuscolo





// var capitalize = function (stringa) {
//     var stringa = 'coding';
//     var maiuscola = stringa[0].toUpperCase();
//     var resto = stringa.substring(1);
//     var unita = maiuscola + resto;
//     return unita;

// }







function toCapital(stringa) {
    var stringa = 'coding';
    var maiuscola = stringa[0].toUpperCase();
    var resto = stringa.substring(1);
    var unita = maiuscola + resto;
    return unita;

}

